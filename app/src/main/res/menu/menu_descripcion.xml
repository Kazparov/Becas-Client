<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.che.cliente.Descripcion">
    <item
        android:id="@+id/compartir" android:title="@string/menu_compartir"
        android:icon="@android:drawable/ic_menu_share"
        app:showAsAction="always">

        <menu>
            <!--
            <item
                android:id="@+id/facebook" android:title="@string/facebook"
                android:icon="@drawable/ic_facebook"
                app:showAsAction="always"/>
            -->
            <item
                android:id="@+id/twitter" android:title="@string/twitter"
                android:icon="@drawable/ic_twitter"
                app:showAsAction="always"/>
        </menu>
    </item>
</menu>
